<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
<% if (!user) { %>
	<p>
    <a href="/login" class="btn-large waves-effect waves-light" style="text-transform: none;">Twitterでログイン</a>
    </p>
<% } else { %>
	<p>Hello, <%= user.username %>. View your <a href="/profile">profile</a>.</p>
    <div><img src="<%= user.photos[0].value %>"></div>
    <div><%= user.displayName %></div>
<% } %>
  </body>
</html>
